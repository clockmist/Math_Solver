## 方案二

### 1. 新增代码文件
#### 1.1 analyze_data_length.py
计算训练集数据增强之后整体的token长度分布范围, 从而指导qwen_cot_ft.py中超参数MAX_LENGTH的设置

#### 1.2 qwen_cot_ft.py
利用cot-sft-finetune分支生成的数据增强版训练集进行训练

#### 1.3 evaluate_cot.py
在验证集上测试训练效果（因为目前无法提交测试，所以直接在训练集上面取一部分数据进行测试，所以准确率相较于实际可能偏高）


### 2. 可能的改进
- 或许可以在数据增强时控制一下大模型生成CoT的长度，提升其信息密度，或许这样子小模型学起来更有针对性？
- 进一步改进qwen_cot_ft.py的代码，例如自动划分出验证集，每训练一个epoch就测试一遍，更加严谨，也支持早停
- 调整训练超参数
- 题目分类，直接剔除难题

